{
  "name": "chattie",
  "version": "1.0.0",
  "description": "A chat application built with Node.js",
  "main": "index.js",
  "scripts": {
    "start": "node server/server.js",
    "start-server": "nodemon server/server.js",
    "test": "export NODE_ENV=test || SET NODE_ENV=test && jest server/**/*.test.js --detectOpenHandles",
    "test-watch": "nodemon --exec \"npm test\""
  },
  "engines": {
    "node": "8.7.0"
  },
  "author": "Ramon Miklus",
  "license": "ISC",
  "dependencies": {
    "express": "^4.16.3",
    "socket.io": "^2.1.1"
  },
  "devDependencies": {
    "jest": "^23.4.1"
  }
}
